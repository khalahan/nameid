* Allow different signing key (address / GnuPG), where the public key is
  associated somehow to the id-name value.

* Allow setting sreg data directly with special field in value.

* Logo for the webpage.

* Use HTTP requests for JSON-RPC.

* Separate off login code further and write HOWTO for stand-alone servers.
